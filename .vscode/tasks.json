{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildweb",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/UrlShortener/UrlShortener.csproj"
            ]
        }
    ]
}